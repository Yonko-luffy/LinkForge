# LinkForge Complete - Environment Configuration

# REQUIRED: Change this to a secure random string in production
SECRET_KEY=change-this-to-random-secure-string-in-production

# Database file path (SQLite)
DATABASE_URL=linkforge.db

# Flask Environment (development/production)
FLASK_ENV=development
FLASK_DEBUG=True

# Server Port (required for some deployment platforms)
PORT=5000

# Security Settings (production only)
# SESSION_COOKIE_SECURE=True
# SESSION_COOKIE_SAMESITE=Strict

# CORS_ALLOWED_ORIGIN for Flask-CORS
# Set this to your production domain (e.g., https://yourdomain.com)
CORS_ALLOWED_ORIGIN=http://localhost:5000
